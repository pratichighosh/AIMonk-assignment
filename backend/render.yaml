services:
  - type: web
    name: aimonk-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn tags_project.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0